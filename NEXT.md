# 이어서 할 일

마지막 커밋 — 아래층 배타화 · 이평선 판정 정정 · 오늘의 신호

## 지금 상태

```
아래층(사업분류)  대분류 12 → 테마 91 · 편입 2,497종목 · 한 종목은 한 칸에만
                 응집도 0.470 (에프앤가이드 올린 폭 대비 92%) · 무작위 상위5% 넘음 43/89
분리도            잔차 안 0.158 · 밖 -0.001 (2026-08-27, separation.mjs)
배포              https://signo-chi.vercel.app/theme
PDF               SIGNO-전종목-테마작업지.pdf (287쪽) · SIGNO-테마분류기준.pdf (34쪽)
                  ← 둘 다 배타화 전(겹침 허용·2,602종목) 자료다. 지금 데이터와 어긋난다.
```

## 지난번에 한 것 (2026-08-28)

### 1. 아래층을 배타 분류로 굳혔다 — `scripts/theme/exclusive.mjs`

한 종목이 평균 1.71개 테마에 들어 있었다. 코오롱은 9개였다. 겹침의 대부분은
뒷받침 낱말 하나만 걸린 3~4점짜리 배치에서 나왔다(규칙 배치 3,320건 중 59%).

특히 대표종목 뽑기가 망가져 있었다. 3점 배치에 걸리는 것은 사업이 많은 대형
지주사인데 시총순으로 뽑으면 그것들이 1~3위로 올라온다. 소프트웨어·SI 상위
3종목이 삼성에스디에스·두산·LG 였다. 두산의 소프트웨어·SI 점수는 3점이고 매출
비중은 0% 다(건설기계 76%).

```
배치 4,264 → 2,497 · 평균 소속 1.71 → 1.00 · 두 칸 이상 든 종목 0
소프트웨어·SI 대표종목  삼성에스디에스·두산·LG → 현대오토에버·LG씨엔에스·포스코DX
```

칸을 정하는 순서는 **매출 비중(20% 이상) → 작업지 → 낱말 점수 → 동점이면 작은 칸**.
마지막은 동점 200건을 눈으로 보고 정했다 — 풍산은 금속가공(94)이 아니라
방위산업(47), 케이씨에스는 소프트웨어·SI(167)가 아니라 정보보안(63)이다.
큰 칸이 뭉뚱그려 흡수하는 성질이 일관되게 나온다.

**응집도가 0.486 → 0.470 으로, 올린 폭이 108% → 92% 로 내려갔다.** 응집도는
겹침을 허용할수록 유리한 잣대다 — 같은 대형주를 여러 칸에 넣으면 올라간다.
예전 108% 에는 삼성전자·SK하이닉스가 반도체 여러 칸의 상위 12에 동시에 들어가
만든 몫이 섞여 있었다. `evaluate.mjs` 가 배타화 전 데이터를 재고 있어서(지주회사를
58종목으로 셌다) 그것도 같이 고쳤다.

곁가지로 두 가지를 고쳤다.
- 매출 부문 라벨이 표에서 줄이 갈리며 낱말 가운데 빈칸이 끼어 매칭이 실패했다
  (프레스티지바이오파마 "바이오 시밀러 100%"). 넉 자 이상 낱말에만 빈칸을
  눈감아 준다 — 통째로 지우면 "레거시 공정"이 "시공"이 되던 문제가 되살아난다.
- 매출 규칙이 비중 크기를 안 봤다. 넥센타이어가 "기타 임대,렌탈 4%" 한 줄로
  렌탈·구독에 갔다. 문턱 20% 를 뒀다.

### 2. 손보기 30건 — `scripts/theme/overrides.json`

규칙으로 못 고치는 자리를 근거와 함께 못박았다. 후보에 아예 없던 칸
(BGF리테일→유통·편의점, CJ제일제당→음식료, 케이티→통신서비스)과 낱말이 헛걸린
자리(HPSP→반도체 장비, LG화학→정유·석유화학, 삼성물산→건설·건축)다.
코드를 잘못 적으면 엉뚱한 회사를 옮기므로(유진테크를 086390 으로 적었다가
유니테스트를 옮길 뻔했다) 이름이 다르면 건너뛴다.

### 3. 이평선 판정을 바로잡았다 — `lib/score.ts` 의 `maRead`

20일선과 60일선 둘만 보고 20 > 60 이면 "정배열" 이라 적고 있었다. 영원무역은
20일선 아래 -11.6% 인데 정배열로 찍혔다. 1,586종목으로 견주니 배지가 바뀐 종목이
481개(30%)다.

```
정배열 120 → 43 (77종목은 사실 혼조)   역배열 781 → 377 (404종목은 사실 혼조)
```

상태(배열)와 이벤트(크로스)를 나눴다. `maRead` 가 align·alignDays·cross·gap20 을
따로 낸다. 크로스에는 **확인**을 붙인다 — ① 60일선이 실제로 방향을 틀었나
(20거래일 전과 견줌) ② 교차 무렵 거래가 실렸나(앞선 20일 평균의 1.2배).

```
최근 20일 내 골든크로스 621건 중 확인은 38건뿐. 나머지는 폭락 뒤 반등이라
20일선만 뚫고 60일선은 아직 하락 중이다. 예전 규칙은 621건 전부에 만점을 줬다.
```

**종합평가의 주가흐름 10점이 바뀌므로 전 종목 점수가 조금씩 움직였다.**

### 4. 쓰는 자리 — 스크리닝과 오늘의 신호

- 테마 상세에 칩 셋 (전체 / 정배열 / 골든크로스). 확인된 것만 센다.
- 테마 목록 위에 신호 띠, 마감 리포트에 `[ 최근 5거래일 이평선 신호 ]`.
  종목만 늘어놓지 않고 테마로 묶는다 — 신호가 한 테마에 몰렸는지가 값어치 있다.
- 창이 5거래일인 이유: 확인까지 통과하는 것이 하루 한 건꼴이다
  (실측 당일 1 · 이틀 2 · 닷새 5). "오늘" 만 내걸면 대부분의 날이 빈칸이 된다.

### 5. 되채우기 버그 — `lib/baseline.ts` 의 `FACT_MARK`

`missingFrom` 이 `mx:` 키만 보고 있으면 건너뛰었다. 그래서 facts(`sx:`)에 항목을
늘려도 TTL 7일이 지나기 전까지 새 항목이 영영 안 붙었다. 위 3·4번이 그대로
빌 뻔했다. facts 에 표식(`align`)이 없으면 다시 채운다. **새 항목을 더할 때는
`FACT_MARK` 를 그 항목 이름으로 바꾼다.**

### 6. 테마 상세 뒤로가기

`router.replace` 라 상세가 히스토리에 안 남았고, 브라우저 뒤로가기를 누르면
테마 화면을 통째로 벗어났다. `window.history.pushState` + `popstate` 로 바꿨다
(Next 가 라우터와 이어 준다 — `docs/01-app/01-getting-started/04-linking-and-navigating.md`).
돌아온 목록에서는 보던 테마의 대분류를 펼쳐 둔다.

## 남은 일

1. **PDF 2종을 다시 뽑는다** — 지금 사이트와 어긋난다.
   배타화 전(2,602종목·겹침 허용) 자료이고, 기준서가 인용하는 응집도도
   108% → 92% 로 바뀌었다. `evaluate` 를 먼저 돌리고 아래 차례대로.

2. **검수 349건** — `.cache/theme/exclusive-review.json`
   ```
   동점 124 · 작업지↔규칙 어긋남 100 · 매출 절반 미만 81 · 작업지 여러 칸 44
   ```
   이 가운데 **대표종목(테마 내 시총 1~3위)으로 뜨는 것이 48건**이다. 화면에
   실제로 보이는 자리라 여기부터 본다. 나머지는 순위가 낮아 급하지 않다.
   확인된 오배치는 `overrides.json` 에 근거와 함께 적는다.

3. **10종목 미만이 된 칸 12개 — 합칠지 정한다**
   ```
   비만·대사질환 치료제(9) · 보안·경비(9) · 2차전지 음극재(8) · IDC·클라우드(8)
   승강기·산업설비(7) · HBM 밸류체인(6) · 완성차(6) · 타이어(6)
   바이오시밀러(5) · 해운(5) · 폐배터리 재활용(4) · 담배(4)
   ```
   담배·해운·타이어·완성차는 원래 작은 산업이라 그대로 두는 편이 맞다.
   분리도가 이미 지목한 것부터 손대면 된다 — **보안·경비를 없애고**(잔차 -0.020,
   유일한 음수) **HBM 밸류체인을 메모리 반도체에 합친다**(교차 0.495 vs 각자
   0.534·0.498, 사실상 한 테마). 바이오시밀러는 차용률 90% 라 배타화하니 무너졌다.

4. **미배정 163건** — `.cache/theme/manual-unmatched.json`
   갈 테마가 아예 없는 쪽이 많다. 악기·문구·출판·인쇄(삼익악기, 모나미, 양지사) ·
   미술품 경매(서울옥션, 케이옥션) · 소형가전(신일전자, 파세코, 오아, 앱코) ·
   위생용품 · 종합상사. 열 종목이 넘고 같이 움직이는 묶음이면 세울 값어치가 있다.

5. **울타리 밖 배정 48건** — `.cache/theme/manual-outgroup.json`
   씨앗(사진 필름)→섬유·화학소재 · 씨앤에스링크(차량용 내비)→자동차부품 ·
   오상자이엘(IT·농업)→농수축산 · 한라IMS(철도차량 제동장치)→자동차부품.
   케이비아이동국실업은 자동차부품사인데 아직 건설·건축에 있다(사업보고서에
   토목시설물 부문이 매출 24억으로 적혀 있다). 작업지에서 손대는 편이 맞다.

6. **윗층(이야기 테마)** — 실험은 끝났고 붙이는 일만 남았다.
   `scripts/research/theme-upper-board.mjs` · `theme-upper-backtest.mjs`
   자세한 내용은 아래 "윗층" 절.

## 윗층 — 재 봤고, 될 물건이다 (2026-08-28)

아래층은 "이 회사가 무엇으로 버는가"(사업보고서·분기 갱신·배타)이고, 윗층은
"지금 왜 같이 움직이는가"(시세·수시 갱신·중복 허용)다. **층을 가르는 것은
겹침 허용 여부가 아니라 출처와 갱신 주기다.**

### 근거는 문서가 아니라 측정이다

`dict.mjs` 의 `EXCLUDED` 는 사건형·정치인·밸류업을 "사업보고서에 근거가 없다"고
뺐다. 그런데 잔차 상관으로 재 보니 그 판단이 거꾸로였다.

```
2026 상반기 신규상장 -0.010 · CCUS -0.029 · 액침냉각 0.021 · 희토류 0.005
지역화폐 0.237 · 스페이스X 0.167 · 코리아 밸류업 0.156 · 딥페이크 0.089
                                     (무작위 12종목의 상위 5% 경계 = 0.043~0.053)
```

사실로 판정 가능한 것이 실재하는 테마인 것은 아니고, 근거를 못 대는 것이
허깨비인 것도 아니다. **돈이 들어왔는지가 테마의 실재를 정한다.**
그래서 윗층의 입장 기준은 두 가지다.

```
실재하는가   잔차 상관 > 무작위 상위 5%       (시장 공통분을 회귀로 걷어낸 뒤)
돈이 왔는가   최근 10일/직전 50일 거래대금 배수 ÷ 시장 배수 > 1.00
규모         종목 5~40 (넘으면 테마가 아니라 업종)
```

거래대금도 시장 몫을 걷어내야 한다. 이 구간의 시장 전체 배수가 0.61 이라
안 나누면 살아 있는 테마까지 ×0.3~0.7 로 찍혀 111건 중 11건만 걸렸다.
잔차 상관에서 겪은 것과 같은 함정이다.

### 네이버와 견줌

```
             칸 수   고유종목   평균 소속   중앙 크기
네이버 업종     79     4,030      1.00      18  (분류 못 한 "기타" 1,237)
네이버 테마    267     2,354      2.82      17
SIGNO         91     2,497      1.00      40  (배타화 뒤)
```

네이버는 두 층으로 나눠 놨다. 업종은 배타, 테마는 중복 전제(삼성전자 31칸).
테마 이름이 `지역화폐`·`밸류업`·`제습기`·`아이폰` 처럼 사업 분류가 아니라
뉴스 소재다. 네이버 256개 중 199개(78%)가 무작위 경계를 넘었다 — 노이즈가
아니다. SIGNO 는 84%, 중앙값은 0.144 대 0.116 으로 앞선다.

네이버 목록은 **후보 풀**로 쓴다. 베끼는 것이 아니다 — 344개 후보 중 실제로
남는 것은 31개고, 초전도체·희토류·콜드체인·CCUS·신규상장은 떨어진다.

### 갱신 주기 — 주 1회

10주치(5거래일 간격)를 되짚었다.

```
주 단위(5거래일)   상위 20 중 15.6개 유지 (78%) · 순위상관 0.930
한 달(20거래일)    상위 20 중 10.5개 유지 (53%)
판 크기            110~133 로 안정
```

매일 돌리면 78% 가 그대로라 화면이 거의 안 바뀌고, 한 달이면 절반이 뒤집혀
늦다. 45개 테마가 상위 20을 돌아가며 차지하고 그중 6개만 한 번 스쳤다 —
**고정 명단이 아니라 회전 목록**이라는 설계가 데이터로 확인됐다.

### 중복은 두 번 뗀다

종목 겹침만으로는 안 떼진다. 네이버 `겨울`(22종목)은 구성이 제약주 위주라
잔차 0.307 로 멀쩡히 통과하는데 백신 테마와 이름만 다르다. `separation.mjs` 의
분리도를 판 안에 그대로 쓴다 — 겹침도(밖/안)가 1 을 넘으면 뗀다. 경계 1.0 은
실측이다.

```
중복  전력기기·전선↔전선 1.17 · PCB↔반도체 기판 1.15 · mRNA↔모더나 1.05
별개  겨울↔바이오시밀러 0.89 · LPG↔해운 0.83 · 환율하락↔해운 0.69
```

### 붙일 때 남은 숙제

- `겨울`·`여름` 처럼 이름이 아무것도 안 알려주고 구성이 엉성한 것이 섞인다
  (겨울 대표종목에 한국가스공사). 측정으로 덮지 말고 사람이 이름·구성을 손봐야 한다.
- 화면·데이터를 아래층과 나눠야 한다 — `stories.json` + 종목 칩 + 주 1회 크론.
- 안 만들기로 한 것: 정치인 관련주. 근거가 소문이다.

## 파이프라인 차례 (순서를 지켜야 한다)

```
collect.mjs       DART 사업보고서 수집      → overview.json
build-df.mjs      낱말 빈도                → df.json      ← 사전을 고쳤으면 반드시
classify.mjs      규칙 분류                → classified.json
ingest-xlsx.mjs   작업지 읽기 <xlsx경로>    → manual.json · manual-unmatched.json
                                            · manual-outgroup.json
tidy-why.mjs      군더더기 제거 --write     (manual.json 고침)
strip-opinion.mjs 투자 평가 제거 --write    (manual.json 고침)
apply-manual.mjs  사람 것을 규칙 위에 얹기  (classified.json 고침)
build-data.mjs    화면용 데이터            → src/data/themes.json
                  ← exclusive.mjs 로 배타화한 뒤 쓴다. --overlap 이면 예전(겹침) 방식.
                    손보기는 overrides.json, 검수 목록은 exclusive-review.json.
evaluate.mjs      응집도 재기              → report-doc.txt  ← 배타화한 것을 잰다
separation.mjs    테마끼리 갈라지는가        → report-separation.txt  ← 따로 돌려도 된다
                                            (일봉은 bars.json 에 그날치를 남긴다)

PDF 2종 (반드시 evaluate 를 먼저 돌린다 — 기준서가 응집도를 거기서 긁어 간다)
doc-data.mjs → make-pdf.py       기준서    SIGNO-테마분류기준.pdf
doc-all.mjs  → make-pdf-all.py   작업지    SIGNO-전종목-테마작업지.pdf
```

작업지 xlsx 는 `~/Downloads/SIGNO_전종목_테마작업지_완성_1.xlsx` 를 쓴다.
(`_2` 도 내용이 같다. 접미사 없는 것은 예전 판이라 배정이 887건뿐이다.)

### 조사용 스크립트 (`scripts/research/`)

```
theme-top3-drawdown.mjs  테마별 시총 상위 3종목의 고점대비·저점대비 (시세 수집·계산)
theme-top3-report.mjs    그 결과를 md·csv·txt 세 벌로  → SIGNO-테마별-주요종목-낙폭.*
theme-upper-board.mjs    윗층 테마판 — 잔차 상관 × 상대 거래대금  → SIGNO-윗층-테마판.md
theme-upper-backtest.mjs 그 판이 얼마나 흔들리는가 (갱신 주기 정하기)
```

## 클라우드(모바일 단독) 에서 안 되는 것

`.cache/` 가 gitignore 라 저장소에 없다. `overview.json` 18MB 를 비롯한
파이프라인 입력이 전부 로컬에만 있다. 그래서 클라우드 세션에서는
**분류·PDF 를 다시 돌릴 수 없다.** 화면(UI) 손보기와 코드 수정은 된다.

시세는 KIS 키가 있어야 하는데 `.env.local` 도 저장소에 없다.
키는 옮기지 않는다 — 필요하면 받는 쪽이 따로 발급한다.
