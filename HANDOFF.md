# SIGNO 인수인계 문서

한국·미국 증시 대시보드. Next.js 16 (App Router) · React 19 · Tailwind 4 · Vercel 배포.
운영 주소 https://signo-chi.vercel.app · 저장소 https://github.com/reformer3120-rgb/signo

---

## 0. 인계받은 사람이 먼저 할 일

1. **저장소 접근 권한**을 받는다 (GitHub Collaborator).
2. **API 키는 직접 발급받는다.** 아래 "환경변수" 참고. 기존 키를 넘겨받지 않는다.
3. `npm install` → `.env.local` 작성 → `npm run dev` → http://localhost:3000
4. 키가 하나도 없어도 화면 대부분은 뜬다. 야후·네이버는 인증이 필요 없기 때문.
   KIS 키가 없으면 프로그램매매·선물·투자자수급·미국 주간거래만 비어 보인다.

```bash
npm install && npm run dev
```

---

## 1. 환경변수

`.env.local` (gitignore 되어 있음). Vercel에는 Project Settings → Environment Variables 에 같은 이름으로 넣는다.

| 이름 | 필수 | 용도 | 발급처 |
|---|---|---|---|
| `KIS_APP_KEY` | 아니오 | 프로그램매매·지수선물·투자자수급·NXT·미국 주간거래 | [KIS 오픈API](https://apiportal.koreainvestment.com) |
| `KIS_APP_SECRET` | 아니오 | 위와 동일 | 위와 동일 |
| `KIS_ENV` | 아니오 | `prod`=실전 / `vts`=모의 (기본 `vts`) | — |
| `UPSTASH_REDIS_REST_URL` | 아니오 | 캐시. 없으면 인메모리로 동작 | [Upstash](https://upstash.com) |
| `UPSTASH_REDIS_REST_TOKEN` | 아니오 | 위와 동일 | 위와 동일 |

> **KIS 실전(`prod`) 키가 필요하다.** 모의투자(vts) 환경은 해외주식 시세를 주지 않아
> 미국 주간거래 기능이 동작하지 않는다.

> `VERCEL_GIT_COMMIT_SHA` / `VERCEL_DEPLOYMENT_ID` 는 Vercel이 자동으로 넣는다. 직접 설정하지 않는다.

키 관리 원칙 — **키는 사람 단위로 발급하고, 넘겨주지 않는다.** 이전 담당자의 키가
공유된 적이 있다면 KIS 포털에서 재발급하고 Upstash 토큰도 회전시킨다.

---

## 2. 구조

```
src/
  app/
    page.tsx              한국 대시보드
    market/               한국 시장지표
    stock/                한국 종목
    us/                   미국 대시보드 · us/market · us/stock
    watchlist/            관심종목
    calendar/             경제 캘린더 + 실적발표
    api/                  29개 라우트 (아래 참고)
  components/
    sections/             화면 단위 큰 블록 (IndexSection, SectorSection, UsSection …)
    stock/                종목 화면 카드 (StockDetailCard, FinancialsCard, SectorRankCard …)
    us/                   미국 전용 (UsStockView, UsIndicatorSection, UsCurrency …)
    CandleChart.tsx       모든 차트가 쓰는 공용 캔들 차트
  lib/                    데이터 수집·가공 (서버 전용이 대부분)
```

`app/api/*` 는 전부 얇은 껍데기다. 실제 로직은 `lib/` 에 있고 라우트는 캐시를 씌워 호출만 한다.

### lib 지도

| 파일 | 역할 |
|---|---|
| `naverApi.ts` | 네이버 모바일 JSON API — 지수·업종·시총상위·특징주·투자자수급·국채금리·섹터 종합평가 |
| `naver.ts` | 네이버 차트(일봉/분봉)·등락 종목수 |
| `yahoo.ts` | yahoo-finance2 인스턴스 |
| `us.ts` | 미국 증시 전반 — 지수·섹터·특징주·시총상위·종목상세·재무·시장지표 |
| `usDaytime.ts` | 미국 주간거래 시세 (KIS 해외주식) |
| `kis.ts` | 한국투자증권 어댑터 — 토큰 관리, TR 호출, 재시도 |
| `score.ts` | **섹터 종합평가 채점 규칙. 한국·미국 공용** |
| `session.ts` | **거래 세션 판정. 화면·서버 공용** |
| `marketIndex.ts` | 환율·원자재·가상자산·선물·아시아 증시 |
| `calendar.ts` `earnings.ts` `econKo.ts` | 경제 캘린더 · 실적발표 · 지표명 한글화 |
| `theme.ts` | 네이버 테마(292개) — 섹터 세분화용 |
| `cache.ts` | Redis/인메모리 캐시. **배포마다 키가 갈린다 (§4 참고)** |
| `usKo.ts` | 미국 종목 한글명 사전 + 한글 검색 |
| `useSticky.ts` `useStickyOffset.ts` `watchlist.ts` | 화면 상태 저장 (localStorage) |

---

## 3. 데이터 출처

| 출처 | 쓰는 곳 | 인증 |
|---|---|---|
| 네이버 모바일 API | 국내 시세·수급·업종·국채금리·증시자금 | 불필요 |
| 야후 파이낸스 (`yahoo-finance2`) | 미국·글로벌 전반, 실적발표일 | 불필요 |
| 한국투자증권 (KIS) | 프로그램매매·지수선물·NXT·실시간 수급·미국 주간거래 | 키 필요 |
| finviz `/api/calendar/economic` | 미국 경제지표 | 불필요 |
| ForexFactory `ff_calendar_thisweek.json` | 일본·유럽 경제지표 | 불필요 |

### 확보 실패한 것 (다시 시도하기 전에 읽을 것)

- **investing.com** — 서버 요청을 전면 차단(403). API·임베드 위젯 둘 다 막힘.
- **KOFIA FreeSIS** (미수금·선물예수금) — exbuilder 기반 SPA라 데이터 엔드포인트 확보 실패.
- **한국 경제지표 캘린더** — 무료로 공개된 소스를 찾지 못함. 캘린더에서 한국을 고르면
  실적발표만 나온다.
- **미국 지수의 시간외 시세** — 야후·KIS 모두 제공하지 않음. 지수선물로 갈음하고 있다.

---

## 4. 반드시 알아야 할 함정

작업하다 실제로 사고가 났던 것들이다.

### 캐시와 응답 형태
`lib/cache.ts` 는 캐시 키 앞에 **배포 식별자(`VERCEL_GIT_COMMIT_SHA`)** 를 자동으로 붙인다.
예전에 응답 형태를 바꾸고 캐시 키를 안 올려서, 새 화면 코드에 옛 형태 응답이 내려와
페이지가 통째로 죽은 적이 있다. 지금은 배포가 바뀌면 캐시도 갈리므로 키를 손으로
올릴 필요가 없다. **이 동작을 없애지 말 것.**

### 로컬에서는 안 보이는 버그
로컬은 인메모리 캐시라 서버를 재시작하면 비워진다. 운영은 Redis라 남는다.
캐시 관련 문제는 로컬에서 재현되지 않는다.

### 네이버 업종 목록 페이징
업종 구성종목은 **100건씩 끊어 오고 시총순 정렬이 아니다.** 1페이지만 읽으면
삼성전자·SK하이닉스 같은 대형주가 2페이지로 밀려 비교군에서 통째로 빠진다.
(`sectorRank` 는 전 페이지를 모으도록 고쳐져 있다.)

### 야후 등락률 단위
`quote()` 는 퍼센트(`0.04`), `quoteSummary(price)` 는 소수(`0.0004`) 로 준다.
`sessionOf(x, pctScale)` 의 배율 인자가 이것 때문에 있다.

### 야후 타임스탬프
`earningsTimestampStart` 등은 라이브러리가 **Date 객체**로 준다. 초 단위로 보고
1000을 곱하면 연도가 57000년이 된다.

### 우선주
우선주는 보통주의 애널리스트 목표주가를 그대로 물려받아 상승여력이 부풀려진다.
`isPreferredDuplicate()` 로 랭킹 후보에서 제외한다.

### 섹터 순위 정규화
점수 정규화 기준(min/max)은 **비교군 고정 멤버로만** 만든다. 검색한 종목을 기준에
넣으면 같은 업종인데 검색할 때마다 순위가 달라진다. (`dimScaler`)

### 캔버스는 CSS 변수를 못 읽는다
차트 글꼴을 `var(--font-space-mono)` 로 주면 지정이 통째로 무시되고 기본 글꼴로
그려진다. `chartFont()` 가 실제 글꼴 이름으로 풀어서 넘긴다.

### KIS 초당 호출 제한
`EGW00201` 이 뜨면 재시도한다(`kisGet` 에 내장). 여러 종목을 돌 때는 3~4개씩 끊어
간격을 둔다. 멀티종목 TR(`FHKST11300006`, 30종목/회)이 있으면 그쪽을 쓴다.

### 세션 판정은 뉴욕 시계로
미국 세션을 한국시간으로 못 박으면 서머타임 전환 때 한 시간씩 어긋난다.
`lib/session.ts` 가 뉴욕 현지 시각으로 판정한다.

---

## 5. 알아둘 만한 KIS TR 코드

문서에서 찾기 어려웠던 것들이다.

| TR | 용도 |
|---|---|
| `FHKST11300006` | 멀티종목 시세 (30종목/회) — 개별 조회보다 20배 빠름 |
| `HHPTJ04160200` | 외국인·기관 **장중 추정** 수급 (09:30·10:00·11:20·13:20·14:30) |
| `FHKST03010100` / `FHKST03010200` | 거래소별 일봉 / 분봉 (`J`=KRX, `NX`=NXT, `UN`=통합) |
| `FHMIF10000000` | 지수선물 — 종목코드 = 상품코드 3자리 + `00000` (101=코스피200, 106=코스닥150) |
| `FHPTJ04400000` | 외국인·기관 순매수 상위 (KRX 가집계, **장중에는 0행**) |
| `FHKST01010600` | 거래원(회원사), 장중 실시간 |
| `HHDFS00000300` | 해외주식 현재가 — 주간거래 거래소코드 `BAQ`(나스닥) `BAY`(뉴욕) `BAA`(아멕스·아카) |

---

## 6. 거래 세션

`lib/session.ts` 한 곳에서 판정한다. 화면 배지와 서버 로직이 같은 코드를 쓴다.

**국내 (한국시간)**
```
08:00~09:00  NXT 프리마켓 — KRX 닫힘, NXT만 거래
09:00~15:30  정규장 (KRX + NXT)
15:30~20:00  NXT 애프터마켓 — KRX 닫힘
그 외        장마감 → 대시보드에 코스피200·코스닥150 선물 표시
```

**미국 (뉴욕 시각 / 괄호는 서머타임 기준 한국시간)**
```
04:00~09:30 ET  프리마켓 (18:00~23:30)
09:30~16:00 ET  정규장   (23:30~06:00)
16:00~20:00 ET  애프터   (06:00~10:00)
20:00~04:00 ET  주간거래 (10:00~18:00) — 야후에 없음, KIS로 가져온다
```
정규장 밖에서는 대시보드에 지수선물을 함께 띄운다.

---

## 7. 섹터 종합평가 채점

`lib/score.ts` 에 한국·미국 공용으로 정의되어 있다. **한쪽만 고치면 어긋나므로
가중치를 바꿀 때는 이 파일만 고친다.**

```
재무건전성 28  (ROE 50% · 부채비율 30% · 영업이익률 20%)
밸류       22  (PER 40% · PBR 35% · EPS 25%)
성장성     15
기관비중   12  (한국=외국인 보유비중 / 미국=기관 보유비중)
시가총액   10
주가흐름   10  (기간수익률 75% · 20/60일선 교차 25%)
배당        3
```
한국은 **시장 전체(시총 상위 100종목) 분포 기준**으로 매긴다 — 비교군을 바꿔도
점수가 변하지 않는다(`lib/baseline.ts`). 비교군은 누구와 견줄지(순위)만 정한다.
평가 시점은 **직전 정규장 종가**라 장중에 점수가 출렁이지 않는다.
미국은 아직 비교군 내 상대평가 + 실시간 가격 기준이다.

점수는 화면에 **등급(S+ 최상 ~ E- 최하)** 으로도 표시한다. 구간은 실제 분포에
맞춰 촘촘한 28~87 구간을 5점 폭으로 나누고 양 끝은 넓게 두었다 (`scoreGrade`).

---

## 8. 배포

`main` 에 push 하면 Vercel이 자동 배포한다. 별도 파이프라인 없음.

```bash
npm run build      # 타입 체크까지 수행 — push 전에 반드시 통과시킬 것
```

배포 후 확인할 화면: `/` `/market` `/stock` `/us` `/us/market` `/us/stock`
`/watchlist` `/calendar` — 8개.

---

## 9. 남은 일 / 알려진 제약

- 미수금·선물예수금 (소스 확보 실패, §3 참고)
- 한국 경제지표 캘린더 (무료 소스 없음)
- 미국 지수의 시간외 시세 (제공처 없음 — 선물로 대체 중)
- AI 브리핑 — 미착수. 예전 `.env.example` 에 `ANTHROPIC_API_KEY` 자리만 있었고
  코드는 없다.
- 실적발표 대상은 손으로 고른 한국 36 · 미국 47 종목 (`lib/earnings.ts`).
  넓히려면 그 목록에 추가한다.
- 미국 종목 한글 검색은 사전(`lib/usKo.ts`, 약 270종목)에 있는 것만 찾힌다.

---

## 10. 면책 고지

모든 화면 하단과 마감 리포트 끝에 아래 문구가 들어간다. **지우지 말 것.**

> 본 사이트에서 제공하는 지표는 투자 참고용이며, 투자의 최종 책임은 투자자 본인에게 있습니다.
