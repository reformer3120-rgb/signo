@AGENTS.md

# SIGNO 작업 규칙

한국·미국 증시 대시보드. 운영 https://signo-chi.vercel.app

**작업 전에 [HANDOFF.md](HANDOFF.md) 를 읽을 것** — 구조·데이터 출처·겪은 함정·KIS TR 코드가
정리되어 있다. 구조를 다른 앱에 옮길 때는 [PATTERNS.md](PATTERNS.md).

## 반드시 지킬 것

- **API 키를 출력하거나 커밋하지 않는다.** `.env.local` 과 `.kis-token.json` 은 gitignore.
  커밋 전에 `git status --short` 로 `.env`·token·secret 파일이 섞이지 않았는지 확인한다.
- **`npm run build` 를 통과시킨 뒤 push 한다.** 타입 체크까지 수행한다.
- **모든 화면 하단과 마감 리포트의 면책 고지를 지우지 않는다.**
  "본 사이트에서 제공하는 지표는 투자 참고용이며, 투자의 최종 책임은 투자자 본인에게 있습니다."
- `main` 에 push 하면 Vercel 이 자동 배포한다. 별도 파이프라인 없음.

## 일하는 방식

- 고친 뒤에는 **실제로 확인한다.** 로컬(`npm run dev`)이나 운영 주소를 직접 찔러 보고,
  숫자가 맞는지 눈으로 본다. "고쳤습니다"만 말하지 않는다.
- 커밋 메시지는 한국어로, **무엇을 왜 고쳤는지**와 검증 결과를 적는다.
- 응답 형태를 바꿀 때 캐시 키를 손댈 필요는 없다 — `lib/cache.ts` 가 배포 식별자를
  자동으로 붙인다. 이 동작을 없애지 말 것.

## 자주 틀리는 곳 (자세한 내용은 HANDOFF.md §4)

- 네이버 목록 API는 **한 번에 100건까지만** 준다. 더 요청하면 빈 응답.
- 네이버 업종 구성종목은 100건씩 끊기고 **시총순이 아니다** — 전 페이지를 모아야
  대형주가 빠지지 않는다.
- 야후 등락률 단위가 API마다 다르다 — `quote()` 는 %, `quoteSummary(price)` 는 소수.
- 야후가 배포 서버에서 일부 종목의 **시가총액을 빼고 준다** (`quoteAll` 이 메운다).
- 캔버스(차트)는 **CSS 변수를 못 읽는다** — 실제 글꼴 이름으로 풀어서 넘긴다.
- 서버리스는 응답을 내보내면 곧 멈춘다 — **뒤로 미루는 작업(fire-and-forget)은 안 된다.**

## 종합평가

채점 규칙은 `lib/score.ts` 한 곳에 있고 한국·미국이 공유한다. 가중치를 바꿀 때는
이 파일만 고친다. 점수는 **시장 전체 분포 기준(절대)** 이고 평가 시점은 **직전 정규장 종가**다.
지표는 크론(`/api/cron/metrics`)이 하루 한 번 미리 모아 둔다.

## 키 없이도 개발할 수 있다

야후·네이버는 인증이 필요 없어 화면 대부분이 그냥 뜬다. KIS 키가 없으면
프로그램매매·지수선물·NXT·투자자수급·미국 주간거래만 비어 보인다. 클라우드나 다른
기기에서 작업할 때는 키를 옮기지 말고, 확인이 필요하면 배포된 운영 주소를 쓴다.
