# 종목 화면 구현 명세 — 법인 앱

**차트 탭은 손대지 않고 정보 탭을 목업처럼 다시 짜기 위한** 명세다.
블록 순서대로 적었고, 각 블록마다 **무엇을 그리나 · 어디서 받나 · 다 됐다고
볼 조건**을 붙였다.

기준 API 는 SIGNO(`signo-chi.vercel.app`) 것이고, 응답은 2026-08-31
셀트리온(068270)에서 실제로 받은 값이다. 코스콤으로 갈아끼우는 방법은
`README.md`(PORTING-STOCK-TAB), 두 앱을 어떻게 나눌지는 `합치기-명세.md`.

---

## 0. 어디를 손대나

**차트 탭은 손대지 않는다.** 지금 그대로 둔다 — 차트 + 성장점수 신호등.
**정보 탭을 목업처럼 다시 짠다.**

```
헤더            종목명 · 코드 · 현재가 · 테마칩 · ★        ← 점수 배지만 뺀다
탭              차트 / 정보 / 뉴스 / AI                    ← 그대로

차트 탭         차트(봉주기·지표·드로잉·지지저항)            ← 그대로
                성장점수 신호등                             ← 그대로

정보 탭 ─────────────────────────────────────────────
  1  종목 개요       주요사업 · 평가 · 모멘텀      ← 새로 붙임 (SIGNO)
  2  종목 상세       수급 · 기간수익률 · 지표 12칸  ← 새로 붙임 (SIGNO)
                     기존 '기업 정보' 카드를 이것이 대신한다
  3  재무제표        연간/분기 · 컨센서스           ← 새로 붙임 (SIGNO)
  4  유동물량        상장·유통 주식수 · 유통비율     ← 그대로
  5  대주주 현황     상위 5                        ← 그대로
  6  섹터 종합평가   등급 · 7개 항목 · 업종 상위 10  ← 새로 붙임 (SIGNO)
```

**재무 신호등(성장점수)은 정보 탭에 넣지 않는다.** 차트 탭에 이미 있다.
같은 것을 두 탭에 두면 어느 쪽이 최신인지 헷갈린다.

대신 신호등의 `[상세보기]` 버튼이 **정보 탭의 재무제표로 바로 가게** 한다.
탭을 정보로 바꾸고 재무제표 블록까지 스크롤한다(§5).

> **확인이 필요한 것** — 지금 정보 탭에 있는 `투자자별 매매동향` 과
> `수급 추이 30일` 이 목업에 안 보인다. 없앨 것인지, 섹터 종합평가 앞에
> 그대로 둘 것인지 정해야 한다. 이 명세는 **그대로 두는 쪽**으로 적었다 —
> 쓰던 것을 말없이 없애는 편보다 낫다. 뺄 거면 블록만 지우면 된다.

### 기존 '기업 정보' 카드는 없앤다

지금 정보 탭 맨 위의 기업 정보 카드는 새 '종목 상세'와 거의 겹친다.

```
기업 정보(지금)   시가총액 · 52주 최고/최저 · PER · PBR · EPS · 배당수익률 · 섹터 · 테마
종목 상세(새것)   위의 것 전부 + BPS · 외국인비율 · 목표주가 · 투자의견 · 추정PER
                  + 최근 5거래일 수급 + 기간수익률 5칸
```

같은 값을 두 번 보여줄 이유가 없다. 다만 두 줄은 갈 곳을 따로 정한다.

| 기업 정보의 줄 | 어디로 |
|---|---|
| 시총·52주·PER·PBR·EPS·배당 | 종목 상세가 그대로 받는다 (52주는 여기서 채워진다) |
| 섹터 | 종목 상세 카드 머리에 종목명과 함께 |
| 테마 | **헤더 칩으로 올린다** (§1) — 목업이 그렇게 되어 있다 |

### 수급이 두 군데 나오는 것에 대해

새 '종목 상세'에 `최근 5거래일 수급` 이 있고, 아래에 기존 `투자자별
매매동향`(당일)과 `수급 추이 30일`이 있다. 겹치는 것이 아니라 기간이 다르다.

```
종목 상세     최근 5거래일 · 누가 사고 있나 한 줄 요약
투자자별      당일 · 외국인/기관/개인 금액
수급 추이     30일 · 날짜별 표와 누적
```

짧은 것에서 긴 것으로 내려가는 순서라 그대로 두면 된다.

---

## 1. 헤더

```
셀트리온  [코스피]  068270 ▾                              ★
190,400  ↘ -0.47% (-900원)      바이오시밀러 -0.05%
```

- 종목명 옆 `▾` 를 누르면 종목 검색이 열린다
- `★` 는 관심종목 담기 (기존 동작 그대로)
- **점수 배지는 넣지 않는다.** 목업에는 원형 `98` 이 있었으나 빼기로 했다.
  성장점수(98)와 종합평가(66)가 한 화면에 나란히 뜨면 어느 쪽을 봐야 하는지
  알 수 없다. 두 점수는 각자 제자리에서 이름을 달고 뜬다.

### 테마 칩

```
바이오시밀러 -0.05%
```

`GET /api/stock-themes?code=068270` → SIGNO 테마 분류.
지금 정보 탭의 "테마" 자리에는 표준산업분류(`기초 의약물질 및 생물학적 제제
제조업`)가 들어가 있다. 그것을 이 값으로 바꾼다.

숫자는 그 테마 구성종목의 오늘 등락률 평균이다. 칩을 누르면 같은 테마
종목 목록으로 간다.

**데이터는 `src/data/themes.json` 한 파일에 다 있다.** 시세도 키도 필요 없다.
자세한 것은 `테마탭-인계.md`.

> 됐다고 볼 조건 — 셀트리온에서 `바이오시밀러`, 삼성바이오로직스에서
> `바이오 CDMO` 가 뜬다. 표준산업분류 문구가 화면 어디에도 남아 있지 않다.

---

## 2. 차트

기존 구현을 그대로 쓴다. 목업과 다른 점만 적는다.

```
봉주기   5분 15분 30분 1시간 4시간 일 주 월        (그대로)
지표     SMA(1)▾  BB  RSI  MACD | 패턴(12) | 추세선  FVG  OB   (그대로)
드로잉   추세선 연장선 수평선 수직선 박스 텍스트 측정 복사 지우기  (그대로)
지지저항 저항2 211,326 · 저항1 193,969 · 지지1 184,143 …        (그대로)
읽기줄   26년 8월 28일 시 190,700 고 190,700 저 187,600 종 190,400
         등락 -900(-0.47%) 거 337.21k                          (그대로)
```

분봉은 법인 쪽에서 코스콤 실시간을 집계해 만든 것이다. 과거분이 소급되지
않으므로 이력 깊이가 고르지 않다는 점만 알고 쓴다.

---

## 3. 종목 개요 — 세 칸  ← 새로 붙임

```
셀트리온 개요
  주요사업  항체 바이오시밀러   특허 만료된 바이오의약품의 복제약
            바이오시밀러 5종목   시총 44.36조
  평가      SIGNO 66점 · 매출25년 +17.01% · 이익률 +28.07% · PER 29.0배
            증권가목표 274,621원 +43.60% · 의견 4.0/5
  모멘텀    1개월 +0.37% · 골든크로스 · 외국인 25.6%
```

### 받는 곳

`GET /api/stock-brief?code=068270`

```json
{
  "biz": ["항체 바이오시밀러", "특허 만료된 바이오의약품의 복제약"],
  "themeName": "바이오시밀러",
  "themeCount": 5,
  "themeChg": -0.05,
  "cap": 443601,
  "score": 66,
  "growth": 17.01,
  "opm": 28.07,
  "finYear": 2025,
  "per": 29.03,
  "target": 274621,
  "upside": 43.6,
  "recomm": 4,
  "ret1m": 0.37,
  "cross": "골든크로스",
  "foreign": 25.55
}
```

| 화면 | 필드 | 비고 |
|---|---|---|
| 주요사업 낱말 | `biz[]` | 편입 사유 문장에서 품목만 뽑아 둔 것 |
| 테마 N종목 | `themeName` `themeCount` | |
| 시총 | `cap` | 억원 |
| SIGNO 점수 | `score` | 없으면 그 조각만 지운다 |
| 매출YY | `growth` `finYear` | |
| 이익률 | `opm` | |
| PER | `per` | |
| 증권가목표 | `target` `upside` | 컨센서스 — 무료 출처가 없다 |
| 의견 | `recomm` | 5점 만점 |
| 1개월 | `ret1m` | |
| 이평선 | `cross` | 정배열·역배열·혼조·골든크로스·데드크로스 |
| 외국인 | `foreign` | % |

### 규칙 두 가지

**빈 칸은 "—" 로 채우지 말고 줄째로 지운다.** 자료가 종목마다 다르다.
컨센서스는 시총 상위 2할에만 있고, 점수는 지표 크론이 훑은 종목에만 있다.
빈 칸이 늘어선 화면은 고장난 것처럼 보인다.

**`cross` 는 그대로 적는다.** 확인되지 않은 교차는 API 가 아예 크로스로
내보내지 않고 `혼조` 로 준다. 화면에서 `?` 를 붙이거나 색을 죽일 필요가 없다.

> 됐다고 볼 조건 — 셀트리온에서 세 줄이 다 차고, 컨센서스가 없는 중소형주
> (예: 대양금속)에서는 `증권가목표`·`의견` 조각만 빠진 채 나머지가 정상으로
> 보인다. "—" 가 화면에 남지 않는다.

---

## 4. 종목 상세  ← 새로 붙임

```
종목 상세                                                셀트리온
최근 5거래일 수급  [기관 매수우위]     개인 +5만  외인 -18만  기관 +13만

  1일      1주      1달      6달      1년
 -0.3%   -0.8%   +0.4%   -5.4%   +15.7%

시가총액 44조 3,601억   PER 29.03배    PBR 2.44배      EPS 6,568원
BPS 78,267원          배당수익률 0.38%  외국인비율 25.55%  목표주가 274,621 (+43.6%)
투자의견 4.00 / 5      추정PER 28.36배   52주 최고 258,500  52주 최저 145,200
```

### 받는 곳

`GET /api/stock-detail?code=068270` → `{ detail, returns, bias }`

```json
{
  "detail": {
    "name": "셀트리온", "price": 191300,
    "per": 29.03, "pbr": 2.44, "eps": 6568, "bps": 78267, "cnsPer": 28.36,
    "marketCap": 443601, "marketCapText": "44조 3,601억",
    "foreignRate": "25.55%", "high52": 258500, "low52": 145200,
    "dividendYield": 0.38, "priceTarget": 274621
  },
  "returns": { "d1": -0.31, "w1": -0.83, "m1": 0.37, "m6": -5.4, "y1": 15.71 },
  "bias": {
    "days": 5, "개인": 47747, "외국인": -177710, "기관": 126805,
    "leader": "기관",
    "today": { "date": "20260828", "개인": 31968, "외국인": -34103, "기관": 1908 }
  }
}
```

`bias.leader` 로 `[기관 매수우위]` 배지를 만든다. 단위는 주식 수다.

**52주 최고·최저는 지금 법인 앱 정보 탭에서 `-` 로 비어 있다.** 이 API 가
채워 준다. 일봉만 있으면 계산되는 값이라 따로 받을 필요도 없다.

> 됐다고 볼 조건 — 12칸이 다 차고, 52주 최고/최저에 숫자가 들어간다.
> 수급 배지가 `기관 매수우위` / `외국인 매수우위` / `개인 매수우위` 중
> 하나로 뜬다.

---

## 5. 재무제표  ← 새로 붙임

```
재무제표                                      [연간] 분기     억원 · E=추정

2026.12 가이던스   애널리스트 컨센서스 추정 · 2025.12 대비
 매출액 55,304 +32.9%   영업이익 18,550 +58.8%   당기순이익 15,911 +54.3%
 영업이익률 33.54% +5.47%p   ROE 8.83% +2.89%p   EPS 6,725 +58.7%

항목        2023.12.   2024.12.   2025.12.   2026.12.E
매출액        21,764     35,573     41,625     55,304
영업이익       6,515      4,920     11,685     18,550
당기순이익      5,397      4,189     10,315     15,911
영업이익률     29.93%     13.83%     28.07%     33.54%
순이익률      24.80%     11.77%     24.78%     28.77%
ROE           5.07%      2.46%      5.94%      8.83%
부채비율       16.53      19.77      28.71          -
EPS           3,187      1,703      4,238      6,725
```

### 받는 곳

`GET /api/financials?code=068270`

```json
{
  "periods": [
    { "title": "2023.12.", "cns": false },
    { "title": "2026.12.", "cns": true }
  ],
  "rows": [
    { "title": "매출액", "values": ["21,764", "35,573", "41,625", "55,304"] }
  ]
}
```

`cns: true` 인 열이 컨센서스(추정)다. 머리글에 `E` 를 붙이고 색을 달리해
실적과 구분한다. 구분하지 않으면 추정치를 실적으로 읽는다.

위쪽 가이던스 카드는 마지막 열(추정)과 그 앞 열(실적)의 차이로 만든다.
`%p` 는 비율 항목(영업이익률·ROE)에만 쓴다.

`-` 는 그 기간에 값이 없다는 뜻이므로 0 으로 바꾸지 않는다.

> 됐다고 볼 조건 — 연간/분기 토글이 돌고, 추정 열이 눈으로 구분되며,
> 부채비율 2026.12.E 자리가 `-` 로 남아 있다.

### 차트 탭 신호등의 [상세보기] 가 여기로 온다

차트 탭 재무 신호등의 `[상세보기]` 를 누르면 **정보 탭으로 바꾸고 이 블록까지
스크롤**한다. 지금은 그 버튼이 어디로 가는지 정해져 있지 않다.

```
누르면   탭 = 정보  →  재무제표 블록으로 스크롤
```

블록에 앵커(예: `id="financials"`)를 달아 두면 다른 데서도 부를 수 있다.

> 됐다고 볼 조건 — 차트 탭에서 [상세보기] 를 누르면 정보 탭 재무제표가
> 화면에 들어와 있다. 탭만 바뀌고 맨 위에 머무르면 안 된다.

---

## 6. 섹터 종합평가  ← 새로 붙임

```
섹터 종합평가                                              제약
비교군  [제약]  [바이오시밀러 5]

  ⓐ  셀트리온
      제약 내 1위 / 15종목
      66점 · 섹터 기준 · ROE 5.9% · 부채 29% · 외국인 25.55%

  재무 57  밸류 68  성장 74  시가총액 88  주가흐름 68  외국인비중 43  배당 13

  최근 주가흐름 성적표 68점  [골든크로스 19일]  20일선 -2%
    1주 -0.8%  1달 +0.4%  3달 +6.5%  6달 -5.4%  1년 +15.7%

제약 상위 종목 (종합점수순) · 클릭하면 해당 종목으로 이동
  1  셀트리온        [골든] 흐름 68  일 -0.3% 주 -0.8% 월 +0.4%  ▓▓▓▓  66  A
  2  삼성바이오로직스        흐름 37  일 -6.8% 주 -4.2% 월 -2.7%  ▓▓▓   62  A-
  …
```

### 받는 곳

`GET /api/sector-rank?code=068270`

```json
{
  "industryName": "제약",
  "groupKey": "industry",
  "groups": [{ "key": "industry", "name": "제약", "count": 15 }],
  "total": 15,
  "rank": 1,
  "target": {
    "code": "068270", "name": "셀트리온",
    "roe": 5.9, "debt": 29, "growth": 77.3, "upside": 43.6,
    "foreignRate": 25.55, "per": 28.94, "pbr": 2.43, "div": 0.38,
    "d1": -0.31, "w1": -0.83, "m1": 0.37, "m3": 6.48, "m6": -5.4, "y1": 15.71,
    "maSignal": "골든크로스", "crossDays": 19, "maGap": -2,
    "maWhy": "60일선 상승 중 · 거래량 1.4배"
  },
  "ranked": [ /* 상위 10 · target 과 같은 모양 */ ],
  "baseline": { "ready": false, "samples": 0, "need": 25 }
}
```

### 알아야 할 것 셋

**1. 비교군은 둘이다.** `groups` 에 업종(제약)과 테마(바이오시밀러)가 온다.
칩으로 바꿔 끼우면 순위가 다시 매겨진다. 테마 비교군은 SIGNO 분류를 쓰므로
`themes.json` 이 있어야 한다.

**2. 점수는 비교군과 무관하다.** 시장 전체(시총 상위 100종목) 분포 기준이라
비교군을 바꿔도 66점은 그대로고 **순위만 바뀐다.** 화면 아래에 이 문장을
적어 두는 편이 낫다 — 안 적으면 "비교군 바꿨는데 점수가 왜 그대로냐" 는
질문이 반드시 나온다.

**3. `baseline.ready` 가 false 면 점수를 못 낸다.** 지표 크론이 시장 분포를
아직 못 채운 상태다(`samples < need`). 이때는 카드를 통째로 숨긴다. 0점이나
빈 막대를 그리면 안 된다.

### 이 블록만 준비가 다르다

앞의 세 블록(개요·상세·재무제표)은 한 종목만 부르면 되는데, 이건 **비교군
전체의 재무가 필요하다.** 15종목이면 DART 를 15번 두드려야 하고, 화면을 열
때마다 그러면 하루 2만 건 제한에 금방 걸린다.

SIGNO 는 크론으로 미리 쌓아 두고 화면은 읽기만 한다.

```
/api/cron/metrics   종목별 지표를 모아 Redis 에 쌓는다 (lib/baseline.ts)
화면                쌓인 것만 읽는다 — DART 를 직접 부르지 않는다
```

같은 구조가 없으면 이 블록은 붙이지 않는 편이 낫다. 자세한 것은
`README.md` §7 의 "sectorRank 를 만들 때".

> 됐다고 볼 조건 — 비교군 칩을 바꾸면 순위와 목록이 바뀌고 점수는 그대로다.
> 상위 목록에서 다른 종목을 누르면 그 종목 화면으로 간다. 지표 크론을 돌리기
> 전에는 카드가 아예 안 보인다.

---

## 7. 유동물량 · 대주주 · 투자자 수급 — 그대로

법인 앱에 이미 있다. 새로 붙는 세 블록 아래에 지금 순서대로 둔다.

```
유동물량   상장주식수 232,617,176 · 유통주식수 161,040,870 · 유통비율 69.2%
대주주     (주)셀트리온홀딩스 24.44% (56,448,667주) … 상위 5
투자자별 매매동향   외국인 -65억 · 기관 +3억 · 개인 +60억 (보유 25.6%)
수급 추이 30일     날짜별 표 + 누적순매수
```

**성장점수는 차트 탭에 그대로 둔다.** 목업에는 맨 아래에 있지만, 지금
차트 탭 아래에 붙어 있는 것을 옮길 이유가 없다. 헤더로 올리지도 않는다(§1).

## 8. 붙이는 순서

앞이 될수록 값싸고 눈에 띄는 변화가 크다.

```
1  테마 칩 교체         themes.json 만 있으면 된다. 키·시세 불필요      (반나절)
                        헤더로 올리고, 기업 정보의 테마 줄은 지운다
2  종목 상세로 교체      기업 정보 카드를 종목 상세로 갈아 끼운다         (1~2일)
                        52주 최고/최저가 여기서 채워진다
3  종목 개요 세 칸       정보 탭 맨 위. stock-brief 한 번               (1~2일)
4  재무제표             financials + 가이던스 카드                     (2~3일)
                        + 차트 탭 [상세보기] 딥링크 (§5)
5  섹터 종합평가        sector-rank + 지표 크론                        (1주 이상)
```

1·2 는 데이터가 이미 있어 바로 된다.
3·4 는 코스콤이 못 주는 항목(재무·컨센서스)이 섞여 있어 DART 키가 필요하다.

**5 만 성격이 다르다.** 비교군 전체의 재무가 필요해 지표 크론을 먼저 세워야
한다(§6). 크론 없이 붙이면 화면 한 번 열 때마다 DART 를 열댓 번 두드리게 되고,
하루 2만 건 제한에 금방 걸린다. 1~4 를 먼저 내보내고 5 는 뒤에 붙여도 된다.

---

## 9. 넘어오는 파일

```
src/components/StockBrief.tsx              종목 개요 세 칸
src/components/stock/StockBriefCard.tsx
src/components/stock/StockDetailCard.tsx   종목 상세
src/components/stock/FinancialsCard.tsx    재무제표
src/components/stock/SectorRankCard.tsx    섹터 종합평가
src/components/stock/StockThemeChips.tsx   테마 칩
src/lib/score.ts                           점수·이평선 판정
src/lib/ownTheme.ts + src/data/themes.json 테마
src/app/api/{stock-brief,stock-detail,financials,stock-themes,sector-rank}/route.ts
src/app/api/cron/metrics/route.ts          지표 크론 (섹터 종합평가에 필요)
src/lib/baseline.ts                        크론이 쌓은 것을 읽는다
```

전체 목록은 `파일목록.txt`(40개). Tailwind `@theme` 토큰과 글꼴 설정을
같이 옮기지 않으면 색이 빠진 채로 뜬다 — `README.md` §1.
